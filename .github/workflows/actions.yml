name: GitHub Actions Demo
on:
  schedule:
    - cron: "*/5 * * * *"
jobs:
  Run_codepen_scrapper:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: npm run build
      - name: Start
        run: npm run start
    
     
